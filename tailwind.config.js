/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./public/**/*.html", // Include HTML files in the public folder
    "./src/**/*.{html,js}" // Include other files if necessary
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
